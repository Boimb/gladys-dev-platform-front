import { Text } from 'preact-i18n'

const ViewHeader = () => (
  <div className="view-header">
    <header className="title text-white">
      <p className="mb-0"><Text id='profile.subtitle'/></p>
    </header>
  </div>
);

export default ViewHeader